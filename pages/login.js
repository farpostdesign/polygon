import Layout from '../components/Layout';
import { LoginForm } from '../components/forms';

const Login = () => (
    <Layout navBar={false}>
        <LoginForm />
    </Layout>
);

export default Login;
